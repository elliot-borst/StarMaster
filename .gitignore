# Personal runtime data — never publish these
config.txt
config.json
Backups/

# Build output — binaries are distributed via GitHub Releases, not tracked here
StarMaster.exe
StarMaster-Setup.exe
dist/

# Scratch / temp
_test.ps1
*.bak
